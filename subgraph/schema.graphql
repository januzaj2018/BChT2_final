type GameItem @entity(immutable: false) {
  id: ID!
  tokenId: BigInt!
  owner: Bytes!
  balance: BigInt!
}

type AMMLiquidity @entity(immutable: false) {
  id: ID!
  user: Bytes!
  lpAmount: BigInt!
  reserveX: BigInt!
  reserveY: BigInt!
}

type Swap @entity(immutable: true) {
  id: ID!
  user: Bytes!
  tokenIn: Bytes!
  amountIn: BigInt!
  tokenOut: Bytes!
  amountOut: BigInt!
  timestamp: BigInt!
}

type Craft @entity(immutable: true) {
  id: ID!
  user: Bytes!
  recipeId: BigInt!
  inputs: [BigInt!]!
  outputs: BigInt!
}

type VaultDeposit @entity(immutable: true) {
  id: ID!
  user: Bytes!
  tokenId: BigInt!
  amount: BigInt!
  shares: BigInt!
  timestamp: BigInt!
}

type Proposal @entity(immutable: false) {
  id: ID!
  proposer: Bytes!
  targets: [Bytes!]!
  values: [BigInt!]!
  signatures: [String!]!
  calldatas: [Bytes!]!
  startBlock: BigInt!
  endBlock: BigInt!
  description: String!
  status: String!
}

type Vote @entity(immutable: true) {
  id: ID!
  voter: Bytes!
  proposalId: BigInt!
  support: Int!
  weight: BigInt!
  reason: String!
}
